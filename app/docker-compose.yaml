version: "3.8"

services:
  shiny:
    build: .
    image: methylation-shiny
    ports:
      - "3838:3838"
    volumes:
      - "../files:/opt/app/files"
    #  - .:/opt/app
    networks:
      - main
networks:
  main:
    name: methylation-main
